import Wrapper from "@/layouts/Wrapper";
import { EventDetails } from "@/sections/events-slug.sections";

const page = () => {
  return (
    <Wrapper>
      <EventDetails />
    </Wrapper>
  );
};

export default page;

