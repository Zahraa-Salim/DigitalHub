// File: frontend/src/pages/courses.tsx
// Purpose: Acts as the route entry for the courses page.
// It composes the shared layout with the main section component for this route.

import { Navigate } from "react-router-dom";

const page = () => <Navigate to="/programs" replace />;

export default page;

