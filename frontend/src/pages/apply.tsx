import Wrapper from "@/layouts/Wrapper";
import { Apply } from "@/sections/apply.sections";

const page = () => {
  return (
    <Wrapper>
      <Apply />
    </Wrapper>
  );
};

export default page;
