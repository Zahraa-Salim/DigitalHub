import NotFoundContent from "@/components/inner-pages/error";
import Wrapper from "@/layouts/Wrapper";

const NotFoundPage = () => {
  return (
    <Wrapper>
      <NotFoundContent />
    </Wrapper>
  );
};

export default NotFoundPage;
